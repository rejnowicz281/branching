import { shortenedStoryMock } from "@/lib/utils/story-brancher/mock";

// eslint-disable-next-line @typescript-eslint/no-unused-vars
export const generateShortenedStory = async (storyText: string): Promise<string> => {
    return shortenedStoryMock; // TODO: replace with openai call
};
